package bio.terra.cloudres.azure.resourcemanager.storage;

import bio.terra.cloudres.common.CloudOperation;

/** {@link CloudOperation} for using Azure StorageManager API. */
public enum StorageManagerOperation implements CloudOperation {
  AZURE_CREATE_STORAGE_CONTAINER
}

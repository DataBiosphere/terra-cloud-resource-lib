package bio.terra.cloudres.azure.resourcemanager.containerinstance;

import bio.terra.cloudres.common.CloudOperation;

/** {@link CloudOperation} for using Azure ContainerInstanceManager API. */
public enum ContainerInstanceManagerOperation implements CloudOperation {
  AZURE_CREATE_CONTAINER_INSTANCE
}

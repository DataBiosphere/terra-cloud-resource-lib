package bio.terra.cloudres.azure.resourcemanager.msi;

import bio.terra.cloudres.common.CloudOperation;

/** {@link CloudOperation} for using Azure MsiManager API. */
public enum MsiManagerOperation implements CloudOperation {
    CREATE_USER_ASSIGNED_MANAGED_IDENTITY
}

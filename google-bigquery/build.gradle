version = '0.1.0'

dependencies {
    api project(':common')
    implementation project(':cloud-resource-schema')

    api group: 'com.google.cloud', name: 'google-cloud-bigquery', version: '1.116.0'
}
